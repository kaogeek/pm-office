{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "forceConsistentCasingInFileNames": false,
    "baseUrl": ".",
    "paths": {
      // each packlet we will assign to new alias
      "$*": ["src/packlets/*"],
      "$assets/*": ["public/assets/*"]
    }
  },
  "include": ["src"]
}
